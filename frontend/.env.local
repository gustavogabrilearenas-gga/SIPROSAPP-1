# Ejemplo de variables de entorno para el frontend
# NEXT_PUBLIC_API_URL se usa en el navegador (cliente) para apuntar al backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# NEXT_PUBLIC_API_URL_SERVER se usa cuando Next corre en servidor (docker) y necesita resolver el backend
NEXT_PUBLIC_API_URL_SERVER=http://web:8000

# Puerto del frontend (opcional)
# PORT=3000
