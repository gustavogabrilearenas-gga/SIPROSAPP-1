"""El módulo ``core`` no define modelos concretos.

Los modelos de auditoría se movieron a ``backend.auditoria`` para mantener una
arquitectura modular. Este archivo se mantiene para cumplir con las
convenciones de Django y facilitar futuras extensiones transversales.
"""
